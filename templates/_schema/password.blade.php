<div class="row">
    <div class="six columns">
        <input type="password"
            name="{{ $name }}"
            placeholder="{{ $label }}"
            autocomplete="off"
        />
    </div>
    <div class="six columns">
        <input type="password"
            name="{{ $name }}_confirmation"
            placeholder="{{ $label }} confirmation"
            autocomplete="off" />
    </div>
</div>
